namespace InMemoryServer;

// Forward declaration to fix circular reference
public partial class BattleState
{
    // We added these method signatures here to help with error correction
    // The actual implementations are in BattleState.cs
}
